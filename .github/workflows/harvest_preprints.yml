name: Daily ArXiv Preprint Harvest

on:
  schedule:
    - cron: "20 6 * * *"  # every day 06:20 UTC
  workflow_dispatch:

permissions:
  contents: write   # allow commits from GITHUB_TOKEN

concurrency:
  group: harvest-preprints
  cancel-in-progress: false

jobs:
  harvest:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt || pip install requests feedparser beautifulsoup4 markdownify python-frontmatter

      - name: Run harvester
        run: python tools/harvest_arxiv.py

      - name: Commit & push if changed
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add .
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Auto-harvest preprints $(date '+%Y-%m-%d')"
            git push
          fi
